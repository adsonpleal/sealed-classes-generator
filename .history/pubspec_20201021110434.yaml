name: sealed_classes_generator
description: Sealed Class Generator is a code generator for sealed classess.
version: 0.1.0
author: adson.leal <adsonpleal@gmail.com>
homepage: https://github.com/adsonpleal/mockito-code-generator

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  build: ">=1.3.0 <1.4.0"
  analyzer: ">=0.35.0 <0.40.0"
  source_gen: ^0.9.7+1

dev_dependencies:
  mockito: ^4.0.0 
  flutter_test:
    sdk: flutter
